# Variables de entorno del Servicio Statistics (Sprint 1)
# Copiar este archivo a .env y ajustar los valores segun el entorno.
# NO guardar secretos reales en el repositorio.

# Puerto del servicio (contrato congelado: 4002)
PORT=4002

# URL base del Servicio Library, sin sufijo de ruta (contrato congelado)
SERVICE_LIBRARY_URL=http://localhost:4001

# Entorno de ejecucion: development | production
NODE_ENV=development
